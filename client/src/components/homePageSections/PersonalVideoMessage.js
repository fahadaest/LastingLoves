import Button from '@mui/material/Button';
import Typography from '@mui/material/Typography';

function PersonalVideoMessage() {
    return (
        <section className="min-h-[90vh] bg-pm-message-bg">

        </section >
    );
}

export default PersonalVideoMessage;